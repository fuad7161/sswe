# SSWE — React scaffold

This folder was converted to a minimal Vite + React TypeScript scaffold.

Quick start:

```bash
npm install
npm run dev
```

The app uses a simple hash-based routing: `#roadmap` and `#resources`.
